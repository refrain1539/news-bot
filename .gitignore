# Python のバイトコードキャッシュ(実行のたびに再生成されるためコミット不要)
__pycache__/
*.py[cod]

# 仮想環境
.venv/
venv/

# pytest の生成物
# .pytest_tmp は tests/conftest.py が tmp_path の置き場所として使う
# (Windows の %TEMP% 配下が使えない環境への回避策)
.pytest_cache/
tests/.pytest_tmp/

# 秘密情報(ローカルでの手動実行用。GitHub Actions では Secrets を使う)
.env
.env.local
